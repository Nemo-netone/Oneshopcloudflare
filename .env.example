# Cloudflare Pages MVP 说明
# 本项目第一版前端部署到 Cloudflare Pages，API 使用 Pages Functions，订单保存到 D1。
# 本地敏感信息不要写入仓库；真实 token 请只放在本机或 Cloudflare 控制台中。

# 可选：如果你用 wrangler CLI 自动部署，可以在本机环境变量中设置：
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=

# D1 绑定名固定为 DB，对应 wrangler.toml 中的 [[d1_databases]].binding。
# 不需要在前端暴露数据库连接字符串。
